{
  "name": "personal-site",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "eleventy --input=src",
    "serve": "eleventy --input=src --serve --port=8080",
    "watch": "eleventy --input=src --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
