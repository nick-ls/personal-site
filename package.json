{
  "name": "personal-site",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "eleventy --input=src --output=build",
    "serve": "eleventy --input=src --output=build --serve --port=8080",
    "watch": "eleventy --input=src --output=build --watch"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
